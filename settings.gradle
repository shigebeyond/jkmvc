include 'jkmvc-common', 'jkmvc-orm', 'jkmvc-http', 'jkmvc-example', 'jkmvc-session', 'jkmvc-oauth2'

rootProject.name = 'jkmvc'

rootProject.children.each {
    it.buildFileName = it.name + '.gradle' - 'jkmvc-'
}